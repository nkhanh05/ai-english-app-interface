name: ai_english_application
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  cached_network_image: ^3.4.1
  camera: ^0.12.0+1
  firebase_core: ^4.7.0
  firebase_storage: ^13.3.0
  flutter:
    sdk: flutter
  flutter_tts: ^4.2.5
  http: ^1.6.0
  image: ^4.8.0
  image_picker: ^1.2.1
  mssql_connection: ^3.0.0
  path_provider: ^2.1.5
  sql_conn: ^2.0.0
  tflite_flutter: ^0.12.1
  ultralytics_yolo: ^0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/background.jpg
    - assets/models/yolov8n_int8.tflite
    